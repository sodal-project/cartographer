<table class="min-w-full">
  <thead class="sticky top-0">
    <tr>
      {{#each data.columns}}
        <th
          class="sticky top-0 px-4 py-4 text-left text-sm font-semibold bg-gray-900 text-white"
        >
          <div
            class="absolute bottom-0 left-0 right-0 border-b border-gray-700"
          ></div>
          <span onClick="sort" class="cursor-pointer">
            {{this}}
          </span>
        </th>
      {{/each}}
    </tr>
  </thead>
  <tbody class="divide-y divide-gray-800">
    {{#each data.rows}}
      <tr class="hover:bg-violet-600/10 cursor-pointer" }>
        {{#each this}}
          <td
            class="whitespace-nowrap pl-4 py-4 text-sm font-medium text-white"
          >
            {{this}}
          </td>
        {{/each}}
      </tr>
    {{/each}}
  </tbody>
</table>